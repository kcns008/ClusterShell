apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: github-copilot

resources:
- deployment.yaml

commonLabels:
  app.kubernetes.io/managed-by: clustershell

# Secret generator (uncomment and set token)
# secretGenerator:
# - name: github-copilot-token
#   namespace: github-copilot
#   literals:
#   - token=ghp_YOUR_TOKEN_HERE
